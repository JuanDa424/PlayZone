import 'package:flutter/material.dart';
import 'chat_widget.dart';

class PlayZoneChatWrapper extends StatelessWidget {
  const PlayZoneChatWrapper({super.key});

  @override
  Widget build(BuildContext context) {
    return const ChatWidget();
  }
}
